#include<bits/stdc++.h>
using namespace std;

int main(){
    int testcases;
    cin >> testcases;
    while(testcases--){
        int n,x,y;
        cin >> n >> x >> y;
        vector<int> a(n);
        for(int i=0; i<n; i++){
            cin >> a[i];
        }
    }
    return 0;
}