#include<bits/stdc++.h>
using namespace std;

vector<vector<int>> permute(vector<int>&V){
    
}

int main(){
    int n;
    cin >> n;
    vector<int> V(n);
    for(int i=0;i<n;i++){
        cin >> V[i];
    }
    vector<vector<int>> ans = permute(V);
}