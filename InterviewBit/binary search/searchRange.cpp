#include<bits/stdc++.h>
using namespace std;

int binarySearch(vector<int>&A, int x){
    if(A.size()==0) return -1;
    int start = 0;
    int end = A.size()-1;
    while(start<=end){
        int mid = start + (end-start)/2;
        
    }
}