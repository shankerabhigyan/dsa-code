#include <iostream>
#include <array>
using namespace std;

int main(){
    array<int, 4> arr = {1,2,9,10};
    int size = a.size();
    cout << a.empty() << endl;
    cout << a.at(4) << endl;
    // a.front() and a.back() :: a[0] and a[size-1]
    
}